<UserControl x:Class="WpfSampleApp.View.MenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfSampleApp.View"
             xmlns:vm="clr-namespace:WpfSampleApp.ViewModel.Main;assembly=WpfSampleApp.ViewModel" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <ObjectDataProvider ObjectType="vm:MenuUserControlVM" />
    </UserControl.DataContext>

    <i:Interaction.Triggers>
        <i:EventTrigger>
            <i:InvokeCommandAction Command="{Binding MenuLoadCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <TreeView x:Name="lvwMenu"
              ItemsSource="{Binding Menu}">
        <TreeView.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Name}" />
            </DataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>
